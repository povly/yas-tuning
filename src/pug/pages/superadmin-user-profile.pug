extends ../layout/base

//- Include mixins
include ../mixins/picture
include ../mixins/video
include ../mixins/iframe

block variables
  - var title = 'superadmin User Profile';
  - var description = '';
  - var pageCss = ['index', 'user'];  // Страничные стили /pages/index.scss
  - var pageJs = ['pages/user', 'pages/superadmin/payments'];
  //- - var blocks = ['user'];
  - var libsJs = ['vanilla-calendar-pro'];
  - var libsCss = ['vanilla-calendar-pro'];
block content
  include ../blocks/superadmin/user-profile

block footer
  include ../components/footer/footer
  include ../components/modal/profile-exit
  script.
    document.addEventListener('DOMContentLoaded', function() {
      document.addEventListener('htmx:beforeSwap', function(evt) {
        if (evt.detail.target.id === 'order-modal-container') {
          document.body.style.overflow = 'hidden';
        }
      });

      //- let userTable = document.querySelector('.user__table-container_user');
      //- new SimpleBar(userTable);
    });
